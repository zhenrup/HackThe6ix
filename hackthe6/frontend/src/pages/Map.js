import map from './storeMap.png';

const Create = () => {
  return (
    <div className="create">
      <h2>Store Map</h2>
      <img src={map} alt="Logo" id="map" />
    </div>
  );
};

export default Create;
